// The file contents for the current environment will overwrite these during build.
// The build system defaults to the dev environment which uses `environment.dev.ts`, but if you do
// `ng build --env=prod` then `environment.ts` will be used instead.
// The list of which env maps to which file can be found in `config/webpack.config.js`.
export const Environment = {
    SERVER_URL: `http://open.xxx.com`,
    production: true,
    development: false
};